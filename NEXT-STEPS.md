# Next Steps

## Highest-priority remaining work
1. Add richer frontend forms and browser-side write actions against the stateful API routes.
2. Run integrated smoke testing across the integrated and stateful shells.
3. Clean up or retire older placeholder/demo paths that are no longer primary.
4. Consider swapping JSON persistence for a database-backed layer.

## If preparing for a demo
1. Start the integrated or stateful API shell.
2. Open `/product-home`.
3. Walk the route flow in `docs/ROUTE-MAP.md`.
4. Use `docs/DEMO-READINESS-CHECKLIST.md` and `docs/SMOKE-TEST-PLAYBOOK.md`.

## If preparing for hardening
1. Add automated tests.
2. Add stronger persistence.
3. Add authentication / RBAC.
4. Add CI/CD and deployment validation.
